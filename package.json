{
  "name": "mahfuzar-portfolio",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "dependencies": {
    "nodemailer": "^6.10.1"
  },
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  }
}
